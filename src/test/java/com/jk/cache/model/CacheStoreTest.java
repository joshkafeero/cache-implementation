package com.jk.cache.model;

import com.jk.cache.util.CacheStore;
import org.junit.jupiter.api.Test;

class CacheStoreTest {

    private CacheStore subject;

    @Test
    void addNode_ToEmptyCache() {
        // Add node will return the node that's been added
//        // the size of the cacheStore is not empty
//        subject = new CacheStore();
////        CacheEntry toBeStored = new CacheEntry("Object to be stored");
//        CacheNode cacheNode = new CacheNode(toBeStored);
//        CacheEntry cacheEntry = new CacheEntry();
//
//        subject.addNode();

    }

    @Test
    void ejectNode() {
    }
}